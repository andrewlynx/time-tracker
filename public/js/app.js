/* For this small app I didn't install package managers and used plain JS instead of minified */
$( document ).ready(function() {
    $('.js-datepicker').datepicker({
        format: 'YYYY-MM-DD'
    });
});